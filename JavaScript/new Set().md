Set is use to remove duplicate from an array.

let arr = ['h', 'h']
  let checking = new Set(arr)
  console.log(...checking) //Spread the value to see the Set content

Set Property 
size : it returns the length of the Set.

console.log(checking.size)