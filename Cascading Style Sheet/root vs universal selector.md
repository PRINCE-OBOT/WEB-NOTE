`*` universal selector explicitly set all elements to have the inheritable styling.
`*{
font-family : sans-serif;
}
`

`:root` it set the styling and allows it child element to naturally inherit the styling

`:root{
 font-family: sans-serif;
}`