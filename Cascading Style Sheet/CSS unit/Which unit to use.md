- **For text, use `rem` for consistency.**

- **For layout, use `%` or `fr` for flexibility.**

- **For full-screen sections, use `vw` and `vh`.**

- **For UI elements like buttons and icons, use `px`.**
